{
	"name": "http-controller-abstractions",
	"version": "0.1.0",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"package.json"
	],
	"scripts": {
		"lint": "biome lint .",
		"format": "biome format . --write",
		"build": "tsc -p tsconfig.json",
		"build:watch": "tsc -p tsconfig.json --watch",
		"build:clean": "rm -rf dist"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.7",
		"typescript": "^7.0.2"
	}
}
